#include <raylib.h>

class Player{
    private:
        float x; float y;
        float rotation = 0;

        Texture2D testsprite;

    public:
        Player(float x_pos, float y_pos, Texture2D testsprite);




};